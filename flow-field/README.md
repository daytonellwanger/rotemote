# Flow Field

Particles drift through a vector field driven by 2D Perlin noise: at every point, the noise value picks a direction, and each particle just keeps walking in whatever direction it's currently standing on. Thousands of low-opacity strokes layer on top of each other to reveal the shape of the underlying field.

![Example](example.png)
