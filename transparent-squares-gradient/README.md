# Transparent Squares + Gradient

Combines the concepts from `transparent-squares` with `gradients`. Specifically, uses the coloring from `gradients` with the patterning from `transparent-squares`.

![Example](example.png)